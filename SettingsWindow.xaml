<Window x:Class="PedalTelemetry.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Settings" Height="550" Width="600"
        Background="{StaticResource BackgroundBrush}"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- HID Device Selection -->
        <GroupBox Grid.Row="0" Header="HID Device Selection" 
                 Foreground="White" 
                 BorderBrush="#404040"
                 Margin="0,0,0,10">
            <StackPanel Margin="10">
                <Button Content="Refresh Axes" 
                       Click="RefreshAxes_Click"
                       Width="120" Height="30"
                       Margin="0,0,0,10"
                       Background="#333333"
                       BorderBrush="#555555"
                       Foreground="White"
                       Cursor="Hand"/>

                <StackPanel Orientation="Horizontal" Margin="0,5">
                    <Label Content="Clutch:" Width="120" Foreground="White"/>
                    <ComboBox x:Name="ClutchAxisCombo" Width="250" 
                             Background="#2A2A2A" 
                             Foreground="White"
                             BorderBrush="#444444"
                             Margin="0,0,5,0"
                             SelectionChanged="ClutchAxisCombo_SelectionChanged"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" Margin="0,5">
                    <Label Content="Brake:" Width="120" Foreground="White"/>
                    <ComboBox x:Name="BrakeAxisCombo" Width="250"
                             Background="#2A2A2A" 
                             Foreground="White"
                             BorderBrush="#444444"
                             Margin="0,0,5,0"
                             SelectionChanged="BrakeAxisCombo_SelectionChanged"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" Margin="0,5">
                    <Label Content="Throttle:" Width="120" Foreground="White"/>
                    <ComboBox x:Name="ThrottleAxisCombo" Width="250"
                             Background="#2A2A2A" 
                             Foreground="White"
                             BorderBrush="#444444"
                             Margin="0,0,5,0"
                             SelectionChanged="ThrottleAxisCombo_SelectionChanged"/>
                </StackPanel>
            </StackPanel>
        </GroupBox>

        <!-- Display Settings -->
        <GroupBox Grid.Row="1" Header="Display Settings" 
                 Foreground="White" 
                 BorderBrush="#404040"
                 Margin="0,0,0,10">
            <StackPanel Margin="10">
                <StackPanel Orientation="Horizontal">
                    <Label Content="Trace Duration:" Width="120" Foreground="White"/>
                    <StackPanel Orientation="Horizontal">
                        <Slider x:Name="TraceSecondsSlider" 
                               Minimum="1" Maximum="60" 
                               Width="200" 
                               ValueChanged="TraceSecondsSlider_ValueChanged"
                               TickFrequency="1" IsSnapToTickEnabled="True"/>
                        <Label x:Name="TraceSecondsLabel" 
                              Content="8 seconds" 
                              MinWidth="80"
                              Margin="10,0,0,0"
                              Foreground="White"
                              VerticalAlignment="Center"/>
                    </StackPanel>
                </StackPanel>
            </StackPanel>
        </GroupBox>

        <!-- Color Settings -->
        <GroupBox Grid.Row="2" Header="Color Settings" 
                 Foreground="White" 
                 BorderBrush="#404040"
                 Margin="0,0,0,10">
            <StackPanel Margin="10">
                <StackPanel Orientation="Horizontal" Margin="0,5">
                    <Label Content="Clutch Color:" Width="120" Foreground="White"/>
                    <Button x:Name="ClutchColorBtn" 
                           Width="60" Height="30"
                           Click="ClutchColorBtn_Click"
                           Background="#0085F1"
                           BorderBrush="#555555"
                           Cursor="Hand"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" Margin="0,5">
                    <Label Content="Brake Color:" Width="120" Foreground="White"/>
                    <Button x:Name="BrakeColorBtn" 
                           Width="60" Height="30"
                           Click="BrakeColorBtn_Click"
                           Background="#D80404"
                           BorderBrush="#555555"
                           Cursor="Hand"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" Margin="0,5">
                    <Label Content="Throttle Color:" Width="120" Foreground="White"/>
                    <Button x:Name="ThrottleColorBtn" 
                           Width="60" Height="30"
                           Click="ThrottleColorBtn_Click"
                           Background="#09B61A"
                           BorderBrush="#555555"
                           Cursor="Hand"/>
                </StackPanel>
            </StackPanel>
        </GroupBox>

        <!-- Buttons -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" 
                   HorizontalAlignment="Right" 
                   Margin="0,10,0,0">
            <Button Content="Cancel" 
                   Click="CancelButton_Click"
                   Width="80" Height="30"
                   Margin="0,0,10,0"
                   Background="#333333"
                   BorderBrush="#555555"
                   Foreground="White"
                   Cursor="Hand"/>
            <Button Content="Save" 
                   Click="SaveButton_Click"
                   Width="80" Height="30"
                   Background="#4CAF50"
                   BorderBrush="#555555"
                   Foreground="White"
                   Cursor="Hand"/>
        </StackPanel>
    </Grid>
</Window>
